要求 $F(x)$ 和 $G(x)$ 的卷积，正常来说需要进行对 $F(x)$ 和 $G(x)$ 均进行一次 DFT，然后他们的乘积进行一次 IDFT。但是注意到
\[
\bigl(F(x) + G(x)\,\mathrm{i}\bigr)^2 = F^2(x) - G^2(x) + 2F(x)G(x)\,\mathrm{i},
\]
所以我们将 $F(x)$ 放在实部，$G(x)$ 放在虚部，可以通过一次 DFT 和一次 IDFT 求出 $F(x)G(x)$ 的值，这就是经典的\textbf{三步变两步优化}。